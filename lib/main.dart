import 'package:flutter/material.dart';

void main() {
  runApp(RealStateAppMain());
}

class RealStateAppMain extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Real State App'),
      ),
    );
  }
}
